# UniForge Sidecars

Sidecars are replaceable, independently managed processes. They communicate with the host using versioned JSON Lines over stdin/stdout; stderr is diagnostic output only. Engines and model assets are deliberately not bundled by Stage 0.
