# Speech Sidecar

Candidate engine: sherpa-onnx (adoption level B), with whisper.cpp as an alternative. Only one is selected for production after license and Windows validation. Raw temporary audio is owned by the caller and should be deleted after processing.
