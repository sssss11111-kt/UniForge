# OCR Sidecar

Candidate engine: PaddleOCR (adoption level B). The adapter requires an explicitly provisioned, reviewed runtime and does not download models. Without that runtime it reports `UNAVAILABLE`.
