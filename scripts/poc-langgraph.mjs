// LangGraph is an optional adapter dependency. The Stage 0 boundary PoC intentionally
// does not install it until the dependency/license decision is approved.
console.error('LangGraph PoC unavailable: optional dependency is not installed; adapter boundary remains disabled.');
process.exitCode = 2;
