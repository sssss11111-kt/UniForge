import { describe, expect, it } from 'vitest'; import { ExtractionService } from './extraction-service.js';
describe('ExtractionService',()=>{it('requires citations and approval for AI proposals',()=>{const s=new ExtractionService();const p=s.create({proposal:{id:'p',targetType:'SUMMARY',body:'summary',citations:[{contentId:'c',quote:'quote'}],generatedBy:'AI',label:'AI Generated',approval:'PENDING'},permissions:['knowledge:propose']});expect(p.approval).toBe('PENDING');expect(s.approve({proposalId:'p',permissions:['knowledge:write']}).approval).toBe('APPROVED');});it('fails closed and enforces label',()=>{const s=new ExtractionService();expect(()=>s.create({proposal:{id:'p',targetType:'FACT',body:'x',citations:[],generatedBy:'AI',label:'User Authored',approval:'PENDING'},permissions:['knowledge:propose']})).toThrow('Citation');expect(()=>s.create({proposal:{id:'p',targetType:'FACT',body:'x',citations:[{contentId:'c',quote:'q'}],generatedBy:'AI',label:'User Authored',approval:'PENDING'},permissions:['knowledge:propose']})).toThrow('label');});});
