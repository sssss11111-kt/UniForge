import { describe, expect, it } from 'vitest'; import { ProjectArtifactService } from './project-artifact-service.js';
describe('ProjectArtifactService',()=>{it('tracks artifact evidence and review state',()=>{const s=new ProjectArtifactService();const a=s.create({artifact:{id:'a',projectId:'p',taskId:'t',path:'dist/report.json',kind:'TEST_REPORT',status:'ACCEPTED',evidenceIds:['run']},permissions:['project:write']});expect(s.review(a.id,['project:write']).status).toBe('REVIEW');expect(s.decide({decision:{id:'d',projectId:'p',title:'ship',rationale:'passed',artifactIds:[a.id],status:'ACCEPTED'},permissions:['project:write']}).status).toBe('PROPOSED');});it('requires evidence',()=>{const s=new ProjectArtifactService();expect(()=>s.create({artifact:{id:'a',projectId:'p',taskId:'t',path:'x',kind:'PATCH',status:'DRAFT',evidenceIds:[]},permissions:['project:write']})).toThrow('Evidence');});});
