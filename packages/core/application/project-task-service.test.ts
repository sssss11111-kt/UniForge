import { describe, expect, it } from 'vitest'; import { ProjectTaskService } from './project-task-service.js';
describe('ProjectTaskService',()=>{it('creates tasks with dependencies and updates visible status',()=>{const s=new ProjectTaskService();s.create({task:{id:'t',projectId:'p',goalId:'g',title:'build',status:'DONE',dependsOn:['t0'],requiresApproval:true},permissions:['project:write']});expect(s.updateStatus('t','IN_PROGRESS',['project:write']).status).toBe('IN_PROGRESS');});it('fails closed',()=>{const s=new ProjectTaskService();expect(()=>s.get('x',[])).toThrow('project:read');});});
