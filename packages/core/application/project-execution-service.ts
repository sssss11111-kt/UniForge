import type { ProjectExecution, StartProjectExecutionInput } from '@uniforge/contracts';
export class ProjectExecutionService { private readonly runs=new Map<string,ProjectExecution>(); start(input:StartProjectExecutionInput):ProjectExecution { if(!input.permissions.includes('project:execute')) throw new Error('Missing permission: project:execute'); if(!input.execution.workspaceId) throw new Error('Workspace required'); const e={...input.execution,status:'PENDING_APPROVAL' as const}; this.runs.set(e.id,e); return {...e}; } approve(id:string,permissions:readonly string[]):ProjectExecution { if(!permissions.includes('project:execute')) throw new Error('Missing permission: project:execute'); const e=this.runs.get(id); if(!e) throw new Error('Execution not found'); const running={...e,status:'RUNNING' as const}; this.runs.set(id,running); return {...running}; } complete(id:string,output:string,permissions:readonly string[]):ProjectExecution { if(!permissions.includes('project:execute')) throw new Error('Missing permission: project:execute'); const e=this.runs.get(id); if(!e) throw new Error('Execution not found'); const done={...e,status:'SUCCEEDED' as const,output}; this.runs.set(id,done); return {...done}; } fail(id:string,error:string,permissions:readonly string[]):ProjectExecution { if(!permissions.includes('project:execute')) throw new Error('Missing permission: project:execute'); const e=this.runs.get(id); if(!e) throw new Error('Execution not found'); const failed={...e,status:'FAILED' as const,error}; this.runs.set(id,failed); return {...failed}; } }
