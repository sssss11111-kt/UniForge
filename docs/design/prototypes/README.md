# Prototype Plan

The prototype must demonstrate App Shell navigation, Dashboard, Agent Center approval flow, Course object preview, Project permission boundary, Knowledge offline state, and Settings diagnostics. It uses fixtures only and must label all unavailable integrations. It must not become production business logic.
