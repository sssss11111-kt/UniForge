# Stage 3 Task 3.7 — Knowledge Workspace

Added a typed Knowledge Workspace snapshot through Main IPC and Preload. The workspace exposes safe empty-state counts for Inbox, Topics, Knowledge, Memory, Actions, and pending approvals; Renderer access remains limited to the typed Preload API and the trusted process supplies permissions.

Validation: focused tests, typecheck, lint, unit/integration regression, desktop build, boundaries, documentation links, and `git diff --check`.
